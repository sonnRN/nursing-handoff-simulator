# Source Layout

- `harness/`
  - shared runtime helpers for Node-based testing and automation

The live browser app still uses the root runtime files, while reusable harness code lives under `src/`.
